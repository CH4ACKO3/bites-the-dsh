import { SessionPlaybackController } from './playback-controller.js'

/** Shared browser-side playback service used by both Cordis and Harmony decorators. */
export const playbackController = new SessionPlaybackController()
